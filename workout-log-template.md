# 운동일지 작성 템플릿 (workout-log-template)

`/workout/` 탭(MMA 스킨)에 새 훈련 일지를 추가할 때 참고하는 양식입니다.
이 양식을 지키면 상단 스코어보드(누적 세션/시간/스파링 라운드), 종목 분포 바,
종목 필터가 자동으로 갱신됩니다.

---

## 1. 파일 생성 규칙

| 항목 | 규칙 |
|------|------|
| 위치 | `_workouts/` 폴더 |
| 파일명 | `YYYY-MM-DD-영문-슬러그.md` (예: `2026-08-03-bjj-halfguard.md`) |
| URL | `/workout/YYYY-MM-DD-영문-슬러그/` (파일명이 그대로 URL이 됩니다) |
| 레이아웃 | 지정 불필요 — `_config.yml` 기본값으로 `layout: workout`, `skin: mma` 가 자동 적용 |

---

## 2. Front matter

```yaml
---
title: "그날 배운 것 한 줄로"      # 필수. 날짜가 아니라 '깨달은 것'을 제목으로
date: 2026-08-03                  # 필수. 정렬·통계 기준
discipline: ["주짓수", "MMA"]     # 필수. 아래 표의 이름과 정확히 일치해야 함
duration: 90                      # 필수(통계용). 분 단위 정수
intensity: 3                      # 1~5. 게이지와 색상에 사용
rounds: 4                         # 선택. 스파링 라운드 수 (누적 통계에 합산)
location: "본관 매트"             # 선택
condition: "양호"                 # 선택. 그날 몸 상태 한 단어
weight: 74.2                      # 선택. kg
tags: [BJJ, 가드, 스파링]          # 선택. 하단 태그 칩
summary: "카드에 보일 2줄 요약"    # 목록 카드의 요약문
focus:                            # 선택. 상단 '오늘의 포커스' 체크리스트
  - "오늘 의식적으로 신경 쓴 것 1"
  - "오늘 의식적으로 신경 쓴 것 2"
---
```

### discipline 에 쓸 수 있는 값

`_config.yml` 의 `mma.disciplines` 에 정의된 이름만 배지 색상과 필터가 붙습니다.

| 이름 | 슬러그 | 색상 |
|------|--------|------|
| MMA | `mma` | `#E11D48` |
| 주짓수 | `bjj` | `#3B82F6` |
| 무에타이 | `muaythai` | `#F97316` |
| 레슬링 | `wrestling` | `#10B981` |
| 복싱 | `boxing` | `#A855F7` |
| 컨디셔닝 | `strength` | `#EAB308` |

종목을 새로 추가하려면 `_config.yml` 의 `mma.disciplines` 에 `{ name, slug, color }`
한 줄을 추가하면 필터 칩과 분포 바에 자동으로 반영됩니다.

### intensity 등급

| 값 | 라벨 | 색상 변수 |
|----|------|-----------|
| 1 | 회복 | `--lvl-1` |
| 2 | 가벼움 | `--lvl-2` |
| 3 | 보통 | `--lvl-3` |
| 4 | 높음 | `--lvl-4` |
| 5 | 한계 | `--lvl-5` |

---

## 3. 본문 구조 (권장)

기술 블로그 포스트와 달리, 운동일지는 **짧고 솔직하게**. 4개 섹션이면 충분합니다.

```markdown
## 드릴
그날 반복한 동작을 목록으로. 세트/횟수까지 적어 두면 나중에 비교가 됩니다.

## 스파링 N라운드
표로 정리하면 상세 페이지에서 스코어카드처럼 렌더링됩니다.

| 라운드 | 상대 | 결과 | 메모 |
|---|---|---|---|
| 1 | 블루벨트 A | 패스 허용 2회 | 니 슬라이스에 계속 당함 |

## 오늘의 결론
왜 그렇게 됐는지 한 문단. 인용문(`>`)은 그날의 핵심 한 줄에만 씁니다.

## 다음 세션까지
다음 훈련 전까지 고칠 것. 구체적인 동작 단위로.
```

---

## 4. 작성 톤

- 잘한 것보다 **깨진 지점**을 남깁니다. 나중에 다시 읽을 때 쓸모 있는 건 그쪽입니다.
- "열심히 했다" 같은 감상 대신, 어떤 동작의 어떤 각도가 문제였는지 적습니다.
- 제목은 날짜가 아니라 그날의 결론으로 (`가드 리텐션이 무너지는 순간을 찾다`).

---

## 5. 테마 관련 파일

| 파일 | 역할 |
|------|------|
| `assets/css/mma.scss` | MMA 스킨 전용 스타일 (기본 테마 토큰을 덮어씀) |
| `assets/js/mma.js` | 종목 필터 |
| `_layouts/workout.html` | 일지 상세 페이지 |
| `workout/index.html` | 목록 + 통계 페이지 |
| `_includes/mma-log-card.html` | 목록 카드 |
| `_includes/mma-badge.html` | 종목 배지 |
| `_includes/mma-intensity.html` | 강도 게이지 |

MMA 스킨은 `page.skin: mma` 가 있을 때만 로드됩니다. 다른 탭의 디자인에는 영향을 주지 않습니다.
