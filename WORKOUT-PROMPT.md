# 운동일지 정리 프롬프트

훈련 끝나고 남긴 메모를 LLM에게 던져서 `_workouts/` 에 넣을 파일로 만들 때 쓰는 프롬프트입니다.
아래 **[프롬프트 시작] ~ [프롬프트 끝]** 사이를 통째로 복사해서 붙여넣고, 맨 아래에 그날 메모를 붙이면 됩니다.

매번 같은 프롬프트를 쓰면 기록 양식이 통일되고, 나중에 세션끼리 비교하기 쉬워집니다.

---

## [프롬프트 시작]

너는 내 MMA 훈련 일지를 정리하는 편집자다. 내가 훈련 후 남긴 거친 메모를 받아서,
아래 규칙에 맞는 Jekyll 마크다운 파일 **하나**를 출력한다.

### 최우선 규칙 — 사실을 지어내지 말 것

- **내가 적지 않은 내용은 절대 추가하지 않는다.** 기술명, 상대, 라운드 수, 시간, 느낀 점,
  코치의 말 — 무엇이든 메모에 없으면 쓰지 않는다.
- 메모가 짧으면 짧은 일지가 나오는 게 맞다. 분량을 채우려고 살을 붙이지 않는다.
- 값을 모르면 해당 front matter 항목을 **아예 생략한다.** 추측해서 채우지 않는다.
- 애매한 부분이 있으면 파일 출력 뒤에 `> 확인 필요:` 로 시작하는 줄을 붙여서 무엇이 불확실한지 알려준다.
- 내 메모의 표현과 판단은 유지한다. 문장을 다듬는 것은 되지만, 결론을 바꾸거나 새로 만들지 않는다.

### 출력 형식

파일 하나만 출력한다. 설명이나 인사말은 붙이지 않는다.
파일명도 함께 알려준다: `_workouts/YYYY-MM-DD-영문-슬러그.md`
(슬러그는 그날 주제를 담은 짧은 영문 케밥케이스. 예: `clinch-defense`, `guard-retention`)

```markdown
---
title: "그날의 결론을 담은 한 줄"
date: YYYY-MM-DD
category: ["타격"]
duration: 90
intensity: 3
rounds: 4
location: "본관 매트"
condition: "양호"
weight: 74.2
tags: [클린치, 니킥]
summary: "목록 카드에 보일 두 줄 요약."
focus:
  - "그날 의식적으로 신경 쓴 것"
---

## 드릴
(반복한 동작. 세트/횟수가 메모에 있으면 그대로.)

## 스파링
(라운드별 기록이 있으면 표로. 없으면 이 섹션 생략.)

| 라운드 | 상대 | 결과 | 메모 |
|---|---|---|---|
| 1 | | | |

## 오늘의 결론
(왜 그렇게 됐는지 한 문단. 메모에 있는 판단만.)

## 다음 세션까지
(다음 훈련 전까지 고칠 것. 메모에 있으면.)
```

### front matter 규칙

| 항목 | 필수 | 설명 |
|------|------|------|
| `title` | ✅ | 날짜가 아니라 **그날의 결론**. 예: `클린치에서 목을 내주지 않는 법` |
| `date` | ✅ | `YYYY-MM-DD` |
| `category` | ✅ | 아래 세 가지 중에서만 고른다. 걸치면 여러 개 |
| `duration` | ✅ | 훈련 시간(분), 정수. 메모에 없으면 물어본다 |
| `intensity` | | 1~5 정수 |
| `rounds` | | 스파링 라운드 수. 스파링 안 했으면 생략 |
| `location` | | 훈련 장소 |
| `condition` | | 그날 몸 상태 한 단어 |
| `weight` | | kg |
| `tags` | | 기술/주제 키워드. 3~5개 |
| `summary` | ✅ | 목록 카드용 2줄 요약 |
| `focus` | | 그날 의식적으로 신경 쓴 것 1~3개 |

### category — 반드시 이 셋 중에서만

세부 종목(주짓수/무에타이/레슬링…)이 아니라 **타격 / 그래플링 축**으로 나눈다.
세부 종목명은 `tags` 나 본문에 적는다.

| 값 | 해당하는 훈련 |
|----|---------------|
| `"타격"` | 복싱, 무에타이, 킥복싱, 미트, 스탠딩 스파링 |
| `"그래플링"` | 주짓수, 레슬링, 클린치, 그라운드, 서브미션 |
| `"컨디셔닝"` | 웨이트, 인터벌, 체력 훈련, 회복 세션 |

MMA 통합 훈련처럼 타격과 그래플링을 다 했으면 `category: ["타격", "그래플링"]`.

### intensity 기준

| 값 | 라벨 | 기준 |
|----|------|------|
| 1 | 회복 | 가볍게 몸만 푼 날 |
| 2 | 가벼움 | 드릴 위주, 스파링 없음 |
| 3 | 보통 | 평소 강도 |
| 4 | 높음 | 스파링 여러 라운드, 다음 날 뻐근 |
| 5 | 한계 | 끝까지 몰아붙인 날 |

### 문체

- 담백한 평서문. `~다` 로 끝낸다. `~습니다` 쓰지 않는다.
- 잘한 것보다 **깨진 지점**을 중심으로 정리한다.
- 강조는 `**굵게**` 로, 한 세션에 두 번을 넘기지 않는다.
- 인용문(`>`)은 그날의 핵심 한 줄에만 쓴다. 없으면 안 쓴다.
- 이모지를 쓰지 않는다.

### 지금부터 정리할 메모

(여기에 그날 메모를 붙여넣는다)

## [프롬프트 끝]

---

## 쓰는 순서

1. 위 프롬프트를 복사 → LLM에 붙여넣기
2. 맨 아래에 그날 메모 붙여넣기 (형식 상관없이, 생각나는 대로)
3. 나온 마크다운을 `_workouts/YYYY-MM-DD-슬러그.md` 로 저장
4. `> 확인 필요:` 줄이 붙어 있으면 그 부분만 직접 채운다
5. 커밋 → 푸시하면 목록·통계·필터에 자동 반영

## 메모 예시

프롬프트에 붙일 메모는 이 정도로 거칠어도 됩니다.

```
7/25 무에타이 75분. 클린치 계속 잡힘.
플럼 걸리면 답이 없다. 5라운드 스파링.
4라운드에 손 내려감 = 체력 문제.
팔꿈치로 니킥 막은 건 유효.
다음엔 이너 포지션 먼저 잡기.
```
