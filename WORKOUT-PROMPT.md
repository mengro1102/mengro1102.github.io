# 운동일지 생성 프롬프트 명세 v2

훈련 메모를 `_workouts/` 파일로 변환하는 LLM 프롬프트의 **규격 문서**입니다.
사람이 복붙해서 쓸 수도 있고, 스크립트가 그대로 API에 실어 보낼 수도 있도록
출력 형식을 기계가 파싱 가능한 계약으로 고정했습니다.

- **§1 출력 계약** — 스크립트가 의존하는 부분. 바꾸면 자동화가 깨집니다.
- **§2 프롬프트 본문** — LLM에 그대로 전달하는 텍스트. `<<<MEMO>>>` 자리에 메모를 넣습니다.
- **§3 검증 규칙** — 생성 결과를 커밋 전에 확인하는 항목.
- **§4 자동화 예시** — 로컬 스크립트 구성.

버전이 바뀌면 문서 상단의 `v2`를 올리고, 프롬프트 안의 `SPEC_VERSION`도 같이 올립니다.

**v2 변경점** — 본문을 `중요한 점` / `문제점` / `느낀점 및 고칠 점` 3절 고정으로 바꿨습니다.
날짜마다 절 이름이 달라지던 문제를 없애고, 상세 페이지 상단 목차가 항상 같은 3항목이 되도록
했습니다.

---

## 1. 출력 계약

LLM은 **아래 세 블록만** 출력합니다. 인사말, 설명, 코드펜스(```) 금지.

```
FILE: _workouts/<YYYY-MM-DD>-<slug>.md
<<<BEGIN>>>
<파일 전체 내용 — front matter 포함>
<<<END>>>
REVIEW: <확인이 필요한 항목 한 줄 요약, 없으면 NONE>
```

### 계약 규칙

| 항목 | 규칙 |
|------|------|
| `FILE:` | 첫 줄 고정. 경로는 항상 `_workouts/` 로 시작 |
| `<<<BEGIN>>>` / `<<<END>>>` | 파일 내용의 경계. 이 사이를 그대로 저장하면 유효한 파일 |
| `REVIEW:` | 마지막 줄 고정. 불확실한 값이 있으면 사유, 없으면 정확히 `NONE` |
| 인코딩 | UTF-8, LF 개행 |
| 그 외 출력 | 금지 |

### slug 규칙

- 소문자 영문 + 숫자 + 하이픈만. 2~4단어.
- 그날의 **주제**에서 뽑습니다. 예: `clinch-defense`, `guard-retention`, `standing-grappling-drill`
- 날짜는 이미 앞에 붙으므로 slug에 날짜를 넣지 않습니다.

---

## 2. 프롬프트 본문

아래 `[PROMPT_BEGIN]` ~ `[PROMPT_END]` 사이를 그대로 LLM에 전달합니다.
`<<<MEMO>>>` 자리에 그날 메모를 치환합니다.

### [PROMPT_BEGIN]

SPEC_VERSION: 2

너는 MMA 훈련 일지를 정리하는 편집자다. 아래 규칙에 따라 훈련 메모를 Jekyll 마크다운
파일 하나로 변환한다. 규칙은 우선순위 순이며, 충돌하면 앞선 규칙이 이긴다.

**규칙 0 — 사실을 만들지 않는다 (최우선).**
메모에 없는 내용은 어떤 것도 추가하지 않는다. 기술명, 상대, 라운드 수, 시간, 장소,
느낀 점, 코치의 말 — 메모에 없으면 쓰지 않는다.
- 값을 모르는 front matter 항목은 **행 자체를 생략한다.** 추측값이나 `null`, 빈 문자열을 넣지 않는다.
- 메모가 짧으면 짧은 일지가 정답이다. 분량을 채우려고 살을 붙이지 않는다.
- 메모의 판단과 결론은 유지한다. 문장을 다듬는 것은 허용하되, 결론을 바꾸거나 새로 만들지 않는다.
- 추론이 필요했거나 값이 모호하면 `REVIEW:` 줄에 무엇이 불확실한지 적는다.

**규칙 1 — 출력 형식.**
아래 형식으로만 출력한다. 다른 문장을 앞뒤에 붙이지 않는다. 코드펜스를 쓰지 않는다.

FILE: _workouts/<YYYY-MM-DD>-<slug>.md
<<<BEGIN>>>
---
<front matter>
---

<본문>
<<<END>>>
REVIEW: <불확실한 항목 / 없으면 NONE>

**규칙 2 — front matter 스키마.**
아래 표에 없는 키를 만들지 않는다. 순서도 표와 같게 유지한다.

| 키 | 필수 | 타입 | 제약 |
|----|------|------|------|
| `subtitle` | 필수 | string | 그날의 결론 한 줄. 날짜·요일을 넣지 않는다. 30자 내외. 큰따옴표로 감싼다 |
| `date` | 필수 | date | `YYYY-MM-DD`. 따옴표 없이 |
| `category` | 필수 | string[] | `타격` `그래플링` `컨디셔닝` 중에서만. 배열 표기 `["그래플링"]` |
| `duration` | 필수 | int | 훈련 시간(분). 정규 타임 1개=60, 2개=120 (규칙 3-1). 판단이 안 서면 생략하고 REVIEW에 적는다 |
| `intensity` | 선택 | int | 1~5 |
| `rounds` | 선택 | int | 스파링 라운드 수. 스파링을 안 했으면 생략 |
| `location` | 선택 | string | 훈련 장소 |
| `condition` | 선택 | string | 그날 몸 상태 한 단어 |
| `weight` | 선택 | float | kg |
| `tags` | 선택 | string[] | 세부 종목·기술 키워드 3~6개. `[주짓수, 클린치]` 형태 |
| `summary` | 필수 | string | 목록 카드용 2줄 요약. 80~120자. 큰따옴표로 감싼다 |
| `focus` | 선택 | string[] | 그날 의식적으로 신경 쓴 것 1~3개. YAML 리스트 |

`title` 키는 쓰지 않는다. 제목은 사이트가 `YYYY-MM-DD 운동 기록` 으로 자동 생성하며,
`subtitle` 이 그 아래 부제로 표시된다.

**규칙 3 — category 판정.**
세부 종목이 아니라 타격/그래플링 축으로 분류한다. 세부 종목명은 `tags` 나 본문에 적는다.

| 값 | 포함되는 훈련 |
|----|---------------|
| `타격` | 복싱, 무에타이, 킥복싱, 미트, 스탠딩 타격 스파링 |
| `그래플링` | 주짓수, 레슬링, 클린치, 테이크다운, 그라운드, 서브미션 |
| `컨디셔닝` | 웨이트, 인터벌, 순환운동, 체력 훈련, 회복 세션 |

한 세션이 두 축에 걸치면 둘 다 적는다: `category: ["타격", "그래플링"]`
판단이 애매하면 넓은 쪽을 고르고 REVIEW에 적는다.

**규칙 3-1 — 하루에 두 타임을 한 경우.**
체육관 정규 타임은 **8시부 / 9시부이며 각 1시간**이다. 두 타임을 다 했으면
`duration: 120`, 한 타임이면 `duration: 60`. 메모에 다른 시간이 적혀 있으면 그쪽을 따른다.
- `category` 는 두 타임에서 한 것을 **합집합**으로 적는다.
- `intensity` 가 타임별로 다르면 **더 높은 값**을 front matter 에 쓰고,
  본문 도입부에 타임별 강도를 각각 적는다.
- 본문은 도입부에서 타임 구성을 먼저 밝히고, `---` 로 타임을 구분한다.

**규칙 4 — intensity 판정.** 메모에 강도 언급이 없으면 생략한다.

| 값 | 기준 |
|----|------|
| 1 | 가볍게 몸만 푼 날, 회복 세션 |
| 2 | 드릴 위주, 스파링 없음 |
| 3 | 평소 강도 |
| 4 | 스파링 여러 라운드, 다음 날 뻐근함 언급 |
| 5 | 끝까지 몰아붙임, 탈진 언급 |

**규칙 5 — 본문 구조 (3절 고정).**
본문은 아래 3개 절로만 구성한다. **절 제목과 순서를 절대 바꾸지 않는다.**
상세 페이지 상단 목차가 이 3절을 그대로 읽어 가므로, 이름이 달라지면 목차가 깨진다.

| 순서 | 절 | 담는 내용 |
|------|-----|-----------|
| 0 | (제목 없는 도입) | 타임 구성과 시간 배분. 2줄 이내 |
| 1 | `## 중요한 점` | 그날 배운 기술과 원리. 드릴 내용, 동작 디테일, 연계 관계 |
| 2 | `## 문제점` | 안 된 것, 막힌 지점, 메모가 지적한 취약점·한계 |
| 3 | `## 느낀점 및 고칠 점` | 메모의 판단·해석, 그리고 다음 훈련까지 고칠 것 |

세부 규칙:

- 기술이 여러 개면 `## 중요한 점` 안에서 `### <기술명>` 으로 나눈다. `###` 는 목차에 잡히지 않는다.
- 드릴 목록(종목·시간 배분)은 도입부 바로 아래 또는 `## 중요한 점` 첫머리에 목록으로 넣는다.
- 다음 훈련 목표는 `## 느낀점 및 고칠 점` 마지막에 목록으로 넣는다. 별도 절을 만들지 않는다.
- 스파링 라운드 기록이나 공수 연계는 표로 정리하되, 해당하는 절 안에 둔다.
- **메모에 해당 내용이 전혀 없는 절은 통째로 생략하고 REVIEW 에 적는다.** 채우려고 지어내지 않는다.
- `## 진행한 드릴`, `## 스파링`, `## 오늘의 결론`, `## 다음 세션까지` 같은 이름은 v2 부터 쓰지 않는다.

절 배분이 헷갈릴 때의 기준:

| 메모의 문장 유형 | 들어갈 절 |
|------------------|-----------|
| "~하면 좋다", "~가 필수다", "~로 넘길 수 있다" (기술·원리) | 중요한 점 |
| "~가 안 됐다", "~를 허용했다", "~가 부족하다", "~하면 막힌다" | 문제점 |
| "~인 것 같다", "~하면 좋을 듯", "다음엔 ~", "~를 느꼈다" | 느낀점 및 고칠 점 |

**규칙 6 — 문체.**
- 담백한 평서문, `~다` 로 끝낸다. `~습니다` 를 쓰지 않는다.
- 잘한 것보다 깨진 지점을 중심에 둔다.
- `**굵게**` 는 한 세션에 최대 2회.
- 인용문(`>`)은 그날의 핵심 한 줄에만. 없으면 쓰지 않는다.
- 이모지를 쓰지 않는다.
- 메모에 있는 영어 표현(`stable`, `underhook` 등)은 그대로 둔다.

**규칙 7 — 표기 통일.**
같은 대상을 메모에서 여러 표기로 썼다면 가장 많이 쓴 표기로 통일하고 REVIEW에 적는다.
(예: `발목받히기` 1회 / `발목받치기` 5회 → `발목받치기` 로 통일)

이제 아래 메모를 위 규칙에 따라 변환하라.

MEMO:
<<<MEMO>>>

### [PROMPT_END]

---

## 3. 검증 규칙

커밋 전에 확인합니다. `§4`의 스크립트가 자동으로 검사하도록 만들어도 됩니다.

| # | 검사 | 실패 시 |
|---|------|---------|
| V1 | 파일명이 `_workouts/YYYY-MM-DD-<slug>.md` 형식인가 | 이름 수정 |
| V2 | front matter의 `date` 와 파일명의 날짜가 일치하는가 | 둘 중 하나 수정 |
| V3 | `subtitle`, `date`, `category`, `summary` 가 모두 있는가 | 채우기 |
| V4 | `category` 값이 `타격/그래플링/컨디셔닝` 안에 있는가 | 수정 |
| V5 | `title` 키가 없는가 | 제거 (제목은 자동 생성) |
| V6 | `duration` 이 있는가 | 없으면 직접 입력. 없으면 Mat Time·그래프에 0으로 잡힘 |
| V7 | `intensity` 가 1~5 정수인가 | 수정 |
| V8 | 스키마에 없는 키가 섞이지 않았는가 | 제거 |
| V9 | 본문 `##` 절이 `중요한 점` / `문제점` / `느낀점 및 고칠 점` 뿐이고 순서가 맞는가 | 절 이름·순서 수정 |
| V10 | `REVIEW:` 가 `NONE` 인가 | 아니면 해당 부분을 사람이 확인 |
| V11 | `bundle exec jekyll build` 가 경고 없이 통과하는가 | 오류 수정 |

---

## 4. 자동화 예시

로컬에서 메모 파일 → 일지 파일로 만드는 흐름입니다.
아래는 구조 예시이며, `llm` 부분은 쓰시는 CLI/SDK로 바꾸면 됩니다.

```bash
#!/usr/bin/env bash
# usage: ./newlog.sh memo.txt
set -euo pipefail

REPO="$(git rev-parse --show-toplevel)"
SPEC="$REPO/WORKOUT-PROMPT.md"
MEMO="$(cat "$1")"

# §2 의 [PROMPT_BEGIN] ~ [PROMPT_END] 사이를 잘라내 메모를 치환
PROMPT="$(awk '/^### \[PROMPT_BEGIN\]$/{f=1;next} /^### \[PROMPT_END\]$/{f=0} f' "$SPEC" \
  | python3 -c "import sys; print(sys.stdin.read().replace('<<<MEMO>>>', '''$MEMO'''))")"

OUT="$(llm "$PROMPT")"   # ← 사용하는 LLM CLI 로 교체

# §1 출력 계약 파싱
PATH_LINE="$(printf '%s' "$OUT" | sed -n 's/^FILE: //p' | head -1)"
BODY="$(printf '%s' "$OUT" | awk '/^<<<BEGIN>>>$/{f=1;next} /^<<<END>>>$/{f=0} f')"
REVIEW="$(printf '%s' "$OUT" | sed -n 's/^REVIEW: //p' | head -1)"

[ -n "$PATH_LINE" ] || { echo "FILE 줄 없음"; exit 1; }
[ -n "$BODY" ] || { echo "본문 없음"; exit 1; }

printf '%s\n' "$BODY" > "$REPO/$PATH_LINE"
echo "작성됨: $PATH_LINE"
[ "$REVIEW" = "NONE" ] || echo "확인 필요: $REVIEW"

cd "$REPO" && bundle exec jekyll build >/dev/null && echo "빌드 통과"
```

메모는 형식 없이 거칠어도 됩니다.

```
7/25 무에타이 75분. 클린치 계속 잡힘.
플럼 걸리면 답이 없다. 5라운드 스파링.
4라운드에 손 내려감 = 체력 문제.
팔꿈치로 니킥 막은 건 유효.
다음엔 이너 포지션 먼저 잡기.
```

---

## 5. 기대 출력 예시

위 메모를 규칙대로 변환하면 이런 형태가 나옵니다.

```
FILE: _workouts/2026-07-25-clinch-defense.md
<<<BEGIN>>>
---
subtitle: "클린치는 잡힌 뒤가 아니라 잡히기 전에 푼다"
date: 2026-07-25
category: ["타격"]
duration: 75
rounds: 5
tags: [무에타이, 클린치, 플럼, 니킥]
summary: "플럼에 걸리면 답이 없다는 걸 다섯 라운드 내내 확인했다. 4라운드에 손이 내려간 건 기술이 아니라 체력 문제였다."
focus:
  - "이너 포지션 먼저 잡기"
---

무에타이 클린치 위주 세션. 75분, 스파링 5라운드.

## 중요한 점

- 팔꿈치로 니킥을 막은 것은 유효했다.
- 이너 포지션을 먼저 잡아야 플럼을 내주지 않는다.

## 문제점

5라운드 내내 클린치를 잡혔다. 플럼에 걸리면 대응할 방법이 없었다.
4라운드에 손이 내려갔다.

## 느낀점 및 고칠 점

손이 내려간 것은 기술 문제가 아니라 체력 문제다.

- 이너 포지션을 먼저 잡기

<<<END>>>
REVIEW: NONE
```
