# /workout/ 이미지

체육관 로고를 이 폴더에 넣고 `_config.yml` 의 `workout.gym.logo` 에 경로를 적으면
Gym History 섹션 상단 카드에 표시됩니다.

```yaml
workout:
  gym:
    logo: "/assets/workout/img/tgc-logo.png"
```

비워 두면 TGC 옥타곤 마크가 대신 렌더링됩니다.
정사각형 PNG(512×512 이상) 권장.
